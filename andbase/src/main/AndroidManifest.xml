<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andbase.library">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".app.activity.AbCaptureActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"></activity>

        <activity
            android:name=".app.activity.AbRecoderActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"></activity>

        <activity
            android:name=".app.activity.AbAnalogClockActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"></activity>

        <activity
            android:name=".app.activity.AbImageViewerActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"></activity>

    </application>

</manifest>
